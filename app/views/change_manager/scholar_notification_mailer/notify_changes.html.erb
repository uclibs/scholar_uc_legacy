<p>Dear <%= User.find_by_email(@changes.first.target).name %>:</p>

<% if @changes.first.is_proxy_change? %>
	<%= render 'proxy_changes' %>
<% elsif @changes.first.is_editor_change? %>
	<%= render 'editor_changes' %>
<% end %>

<p>
Thanks,
Scholar@UC
</p>
